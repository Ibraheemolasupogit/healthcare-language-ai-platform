# MLflow Target-State Contract

This directory describes a dry-run MLflow experiment-tracking design for the
rule-based Milestone 5 baseline. It is reference-only: no tracking server,
workspace URL, token, registry credential, or client connection is configured.

The intended target state records dataset lineage, rule versions, metrics,
model-card artifacts, and evaluation evidence produced locally from synthetic
fixtures.

