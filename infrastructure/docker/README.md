# Docker Baseline

The Dockerfile provides a local Python 3.12 runtime for foundation validation.
It installs the package, runs as a non-root user, and exposes no ports.

Not implemented yet: API serving, model serving, orchestration, or cloud
deployment.
