# RAG Evidence Assembly

Evidence is selected only from the approved `abstaining_ensemble_v1` retriever.
Default bounds are five evidence units, two units per document, and 6000 context
characters. Citation labels are deterministic `[E1]`, `[E2]`, and so on.
