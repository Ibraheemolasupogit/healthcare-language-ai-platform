# Retrieval Corpus

The corpus contains document, section, and sentence retrieval units derived from
validated Milestone 4 text. Unit IDs are deterministic and retain document,
section, sentence, subject, encounter, preprocessing, and extraction lineage.

Document units preserve full normalised document text. Section and sentence units
preserve their validated processed text. Evaluations can target one granularity or
the combined `all` fixture.

