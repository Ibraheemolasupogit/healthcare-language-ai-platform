# RAG Architecture

The RAG flow is local and synthetic-only: query safety classification, approved
retrieval validation, evidence assembly, prompt-contract selection,
deterministic generation, citation validation, groundedness validation, safety
validation, and evaluation. It does not call hosted models or clinical systems.
