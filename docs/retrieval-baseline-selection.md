# Retrieval Baseline Selection

The checked-in comparison evaluates model-free configurations and selects `feature_reranked_hybrid_v1` by validation NDCG@5. The selection report records skipped optional model configurations, selected baseline evidence, holdout metrics, and gate results.

Milestone 8 remediation selected `abstaining_ensemble_v1` and approved it only
for future synthetic RAG prototype planning. Milestone 9 validates that approval
before each guarded RAG run.
