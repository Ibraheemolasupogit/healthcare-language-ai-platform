# Retrieval Model Card

The retrieval baseline combines keyword, TF-IDF, BM25, deterministic hash vectors,
metadata filtering, and hybrid fusion. It is not a clinical search product and
does not generate medical advice or answers.

The hash-vector baseline is not a language model.

