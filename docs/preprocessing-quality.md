# Preprocessing Quality

Quality checks cover empty text, very short text, Unicode replacement
characters, section coverage, long sentences, and annotation projection success.

Checks produce `passed`, `warning`, or `failed` statuses. Overall report and
reconciliation statuses are derived from individual checks.
