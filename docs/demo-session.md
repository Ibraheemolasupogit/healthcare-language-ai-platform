# Demo Session

The demo runner executes deterministic fixture-backed scenarios through the shared query service. Reports are written to `reports/demo/` and checked-in fixture evidence is stored under `tests/fixtures/demo/`.

Reports include scenario ID, query ID, expected and actual status, retrieval status, citation count, groundedness outcome, safety outcome, pass/fail, and trace index. Full clinical text is excluded.
