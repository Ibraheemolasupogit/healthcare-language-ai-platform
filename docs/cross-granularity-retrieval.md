# Cross-Granularity Retrieval

Milestone 7 compares document, section, and sentence retrieval policies. Supported registry values include `single_granularity`, `parallel_granularity`, and `hierarchical`.

Parallel retrieval fuses normalized evidence across unit types. Hierarchical retrieval records parent-child intent and favors target granularity without assembling prompts or generating answers.
