# RAG Prompt Contracts

Prompt files in `models/prompts/rag` are local contracts. They specify allowed
inputs, required outputs, citation format, refusal rules, prohibited content and
size limits. They contain no credentials or provider-specific secrets.
