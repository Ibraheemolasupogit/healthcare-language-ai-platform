# Readiness and Health

Liveness confirms process availability only.

Readiness checks configuration loading, synthetic-only mode, retrieval approval, RAG approval, approved retrieval configuration, prompt assets, fixture evidence, service dependencies, and writable operational output directory.

Readiness does not depend on network access, cloud services, hosted models, or external databases.
