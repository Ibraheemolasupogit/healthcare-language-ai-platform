# RAG Citation Validation

Citation validation checks label uniqueness, evidence foreign keys, quoted span
matching, source checksums, claim citation coverage and deterministic ordering.
Repair is bounded and never invents evidence.
