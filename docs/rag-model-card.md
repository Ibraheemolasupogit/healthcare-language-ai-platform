# RAG Model Card

The generated model card describes the synthetic system type, retrieval
baseline, deterministic generator, prompt contracts, citation contract,
groundedness method, refusal policy, safety controls, metrics, limitations and
unsupported uses.
